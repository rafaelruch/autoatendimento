# ===========================================
# CONFIGURAÇÃO DE PRODUÇÃO - AUTO ATENDIMENTO
# ===========================================
# Sistema Multi-Tenant SaaS
# As credenciais de pagamento são configuradas POR LOJA no painel admin

# ===========================================
# DATABASE (PostgreSQL)
# ===========================================
# Em produção, use uma URL segura com SSL
DATABASE_URL="postgresql://user:password@host:5432/autoatendimento?schema=public"

# ===========================================
# AUTENTICAÇÃO
# ===========================================
# IMPORTANTE: gere uma chave forte em produção
# Use: openssl rand -base64 32
JWT_SECRET="sua-chave-secreta-aqui-minimo-32-caracteres"

# ===========================================
# SERVIDOR
# ===========================================
PORT=3001
NODE_ENV="production"

# URLs (ajuste para seu domínio em produção)
FRONTEND_URL="https://seu-dominio.com.br"
BACKEND_URL="https://api.seu-dominio.com.br"

# ===========================================
# PAGAMENTOS - CONFIGURAÇÃO GLOBAL
# ===========================================
# As credenciais de Mercado Pago e PagBank são configuradas
# POR LOJA no painel administrativo de cada loja.
#
# Aqui ficam apenas configurações globais da plataforma:

# PagBank: usar sandbox (true) ou produção (false)
# Em produção, defina como "false"
PAGBANK_SANDBOX="false"

# ===========================================
# UPLOAD DE ARQUIVOS
# ===========================================
UPLOAD_MAX_SIZE_MB=5
UPLOAD_DIR="uploads"
